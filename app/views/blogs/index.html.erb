<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blogs</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <!-- Font Awesome CDN -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <!-- Bootstrap JS -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.1.3/js/bootstrap.bundle.min.js"></script>
</head>
<body>
  <div class="container mt-5">
    <% if notice.present? %>
      <p class="notice alert alert-success"><%= notice %></p>
    <% end %>
    <h1 class="mb-4">Blogs</h1>

    <div id="blogs" class="row">
      <% @blogs.each do |blog| %>
        <div class="col-md-4 mb-4">
          <div class="card">
            <% if blog.photo.attached? %>
              <%= image_tag url_for(blog.photo), class: "card-img-top", alt: blog.title %>
            <% else %>
              <%= image_tag "https://upload.wikimedia.org/wikipedia/commons/7/7c/Profile_avatar_placeholder_large.png?20150327203541", class: "card-img-top", alt: blog.title %>
            <% end %>
            <div class="card-body">
              <h5 class="card-title"><%= blog.title %></h5>
              <div class="truncate-text">
                <%= blog.content %>
              </div>
              <button type="button" class="btn btn-link p-0" data-bs-toggle="modal" data-bs-target="#blogModal-<%= blog.id %>">Read more</button>
              <div class="actions mt-3">
                <p>
                  <%= link_to "Show this blog", blog, class: "btn btn-primary btn-sm d-block mb-2" %>
                </p>
                <div class="d-flex justify-content-between">
                  <%= link_to "Edit", edit_blog_path(blog), class: "btn btn-outline-secondary btn-sm me-2" %>
                  <%= button_to blog, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-outline-danger btn-sm" do %>
                    <i class="fa-solid fa-trash"></i> Apagar
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Modal -->
        <div class="modal fade" id="blogModal-<%= blog.id %>" tabindex="-1" aria-labelledby="blogModalLabel-<%= blog.id %>" aria-hidden="true">
          <div class="modal-dialog">
            <div class="modal-content">
              <div class="modal-header">
                <h5 class="modal-title" id="blogModalLabel-<%= blog.id %>"><%= blog.title %></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
              </div>
              <div class="modal-body">
                <%= blog.content %>
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>

    <div class="new-blog mt-4">
      <%= link_to "New blog", new_blog_path, class: "btn btn-primary" %>
    </div>
  </div>
</body>
</html>
