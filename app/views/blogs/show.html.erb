<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @blog.title %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <!-- Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome CDN -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    body {
      font-family: 'Roboto', sans-serif;
    }

    .blog-post {
      background-color: #f9f9f9;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .blog-post h1 {
      font-size: 2.5rem;
      font-weight: 700;
    }

    .blog-post p.lead {
      font-size: 1.2rem;
      line-height: 1.6;
    }

    .btn-outline-custom {
      border-color: #007bff;
      color: #007bff;
    }

    .btn-outline-custom:hover {
      background-color: #007bff;
      color: white;
    }

    .actions .btn {
      margin: 5px;
    }

    .new-blog-link {
      font-size: 1.2rem;
      font-weight: 500;
    }

    .container {
      max-width: 800px;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <% if notice.present? %>
      <div class="alert alert-success">
        <%= notice %>
      </div>
    <% end %>

    <div class="blog-post mb-4">
      <h1 class="text-center"><%= @blog.title %></h1>
      <hr>
      <p class="lead"><%= @blog.content %></p>
    </div>

    <div class="actions d-flex justify-content-center mb-4">
      <%= link_to "Editar ou atualizar", edit_blog_path(@blog), class: "btn btn-outline-custom mx-2" %>
      <%= link_to "Voltar para o Blog", blogs_path, class: "btn btn-secondary mx-2" %>
      <%= button_to @blog, method: :delete, data: { confirm: 'Tem certeza?' }, class: "btn btn-danger mx-2" do %>
        <i class="fa-solid fa-trash"></i> Apagar
      <% end %>
    </div>

    <div class="text-center">
      <%= link_to "Novo Blog", new_blog_path, class: "btn btn-primary new-blog-link" %>
    </div>
  </div>

  <!-- Bootstrap JS, Popper.js, and jQuery -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.min.js"></script>
  <%= javascript_include_tag 'application' %>
</body>
</html>
